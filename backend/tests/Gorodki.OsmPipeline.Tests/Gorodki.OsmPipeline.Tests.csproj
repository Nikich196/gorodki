<Project Sdk="Microsoft.NET.Sdk">
  <!-- Конвейер OSM на синтетике (без данных OSM и без osmium): темы, маски, «достижимое», растр, районы, Арена, файл набора. -->
  <PropertyGroup>
    <!-- xUnit v3: тестовый проект — это исполняемый файл. -->
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\tools\Gorodki.OsmPipeline\Gorodki.OsmPipeline.csproj" />
  </ItemGroup>
</Project>
