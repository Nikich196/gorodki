# Егор, это для тебя: что готово, что на тебе и как это сделать

> Для Егора (GitHub `trinotox64`). Составлено 23.09.2026, обновлено 24.09 по [PLAN.md](../PLAN.md) и [JOURNAL.md](../JOURNAL.md).
> Если что-то здесь расходится с планом — прав план, а этот файл надо поправить.
> Все твои задачи — [открытые задачи, назначенные на тебя](https://github.com/Nikich196/gorodki/issues?q=is%3Aopen+assignee%3Atrinotox64)
> (метка [`для-друга`](https://github.com/Nikich196/gorodki/labels/для-друга)).

## 1. Кто ты в проекте

- «Городки» — семестровый проект по мобильной разработке (БрГТУ): iOS-игра о захвате Бреста. Показ — конец декабря 2026.
- Команда: Никита — продукт, дизайн, iOS; **ты — сервер на C#** (ASP.NET Core 10). Основной код пишет Claude (ИИ-ассистент),
  а от нас нужно **понимать код и уметь его защитить** ([PLAN.md](../PLAN.md), §1).
- По плану у тебя **около 2 часов в неделю** (§10). На показе твоя роль — MacBook, таймер, ролики-страховки и сервер (§11).

## 2. Что уже готово

**Сервер** (`backend/`) написан и покрыт тестами, но **пока нигде не развёрнут**. База Supabase готова (#35, раздел 3),
миграции применены; развернуть сервер на Render — задача Никиты ([deploy-render.md](deploy-render.md)).
- Вход: Google → свой JWT на 15 мин + refresh-токен, регистрация по инвайтам ([auth.md](../architecture/auth.md)).
  Пока Никита не завёл Google OAuth, `POST /auth/google` отвечает 503.
- Забеги: телефон шлёт точки кусками, работает без сети, любой запрос можно повторить ([runs.md](../architecture/runs.md)).
- Античит «судья отрезков» — тот же, что на телефоне; совпадение проверяется по общим эталонам ([contracts](../../contracts/README.md)).
- Движок точных участков (спайк S13 «годится», [ADR 0003](../adr/0003-territory-engine.md)), заявки петель и обработчик
  захватов, угасание земли ([captures.md](../architecture/captures.md)), карта `GET /territory` ([territory-map.md](../architecture/territory-map.md)).
- Туман «Исследования» `GET /fog` ([fog.md](../architecture/fog.md)), журнал захватов и откат нарушителя, календарь сезонов
  `GET /seasons`, игровой конфиг `GET /config`.
- Реальное время на SignalR: подсказки «тайлы изменились» и «заявка решена» ([realtime.md](../architecture/realtime.md)).
- Рейтинг «Кто открыл больше» по ежедневному срезу ([fog.md](../architecture/fog.md)), визиты, приватные зоны,
  защита от мультиаккаунтов, удаление аккаунта и «Мои данные» (закон 99-З, [auth.md](../architecture/auth.md)).
- База: PostgreSQL + PostGIS через EF Core с миграциями ([data-model.md](../architecture/data-model.md)).
- Тесты: больше 200 быстрых (xUnit v3) и интеграционные на настоящей базе в Docker (образ Supabase) — их гоняет CI.

**iPhone-часть** (`ios/`, Swift):
- каркас приложения (сборки Free и Paid), экран «Проверка установки», раздел «Лаборатория» (прогулка, карта с туманом),
  Live Activity и виджет;
- пакеты `GameCore` (правила, античит, детектор петли, туман), `GorodkiAPI` (клиент сервера из OpenAPI,
  [ADR 0004](../adr/0004-openapi-swift.md)), `GorodkiSync` (офлайн-очередь забега и фоновая досылка,
  [sync.md](../architecture/sync.md)), `GorodkiNetwork` (токены, вход, реальное время), `GorodkiPersistence` (база на GRDB);
- собрать приложение можно только на Mac. Своего Mac пока нет, поэтому собирает CI (GitHub Actions на macOS),
  а на iPhone его ставят с Windows через Sideloadly ([install-on-iphone.md](install-on-iphone.md));
- экрана входа в приложении ещё нет: логика входа готова, экран ждёт Google Client ID (#4) и дизайн.

Подробности по дням — в [JOURNAL.md](../JOURNAL.md) (новое сверху).

## 3. Твои задачи

По срочности. Время — примерная оценка.

| # | Что | Зачем | Время | Задача | Срок | Зависит от |
|---|---|---|---|---|---|---|
| 1 | Двухфакторная проверка (2FA) на GitHub | Требование плана (§8) для всех с доступом к репозиторию | 10 мин | [#34](https://github.com/Nikich196/gorodki/issues/34) | сразу | — |
| 2 | Supabase: ~~проект базы в EU~~ — сделал Claude 23.09 с твоего входа. **Тебе:** сменить пароль аккаунта Supabase и включить 2FA | Вход в аккаунт передавался в переписке — его нужно сменить | 10 мин | [#35](https://github.com/Nikich196/gorodki/issues/35) | сразу | — |
| 3 | Запустить сервер и тесты у себя | Проверить, что всё собирается; первое знакомство с кодом | 30–60 мин | [#36](https://github.com/Nikich196/gorodki/issues/36) | до 30.09 | — |
| 4 | Первый PR: ты в CODEOWNERS для `backend/` | Пройти путь ветка → PR → CI; GitHub начнёт звать тебя на серверные PR | 20–30 мин | [#37](https://github.com/Nikich196/gorodki/issues/37) | до 30.09 | 3 |
| 5 | Sentry: организация в регионе EU | Отчёты об ошибках приложения (§9) | 15 мин | [#38](https://github.com/Nikich196/gorodki/issues/38) | до 30.09 | 1 |
| 6 | cron-job.org: пингер | Чтобы сервер на Render не засыпал, а Supabase не вставала на паузу | 15 + 10 мин | [#39](https://github.com/Nikich196/gorodki/issues/39) | аккаунт до 30.09, задание — после деплоя | 2 и деплой (Никита) |
| 7 | Спайк S2b: «Городки» на твоём iPhone | Проверить, что игра ставится не только у Никиты | 1–1,5 ч | [#40](https://github.com/Nikich196/gorodki/issues/40) | 01.10–14.10 | S2 у Никиты (#1), iPhone на iOS 26+ |
| 8 | Разобраться, как забег превращается в землю | Тебе рассказывать про сервер на защите | 1–2 ч частями | [#41](https://github.com/Nikich196/gorodki/issues/41) | до 14.10 | 3 |

Честно: на этап 0 план отводит тебе 1 час, а выходит около двух. Не хватает времени — делай 1 и 2: это безопасность аккаунтов.

**Потом — задачи по коду** (§8): по одной в две недели, каждая до 1,5 ч, никогда не на критическом пути.

| Задача | Этап / срок | Состояние |
|---|---|---|
| Согласие на показ профиля: `PUT /me/public-profile` | 2 / 04.11 | **Готова к работе — [#71](https://github.com/Nikich196/gorodki/issues/71)**: тесты и заглушка в `main` |
| ~~Эндпоинт «Мои данные»~~ | — | Сделал Claude раньше срока: понадобился вместе с удалением аккаунта (закон 99-З). Разбери решение: `Features/Me/AccountExport.cs` |
| ~~Рейтинги «Исследования» по снимку~~ | — | Сделал Claude: Must Сезона 0. Разбери: `Features/Leaderboards` |
| Зал славы (эндпоинт) | 3b / 30.11 | Ждёт правил от Никиты: что в нём и за какой срок |
| Итоги дуэлей (задача Hangfire) + тесты ставок | 4 / Д−14 (при показе 22.12 — 08.12) | Позже |
| Сценарии нагрузки k6 + отчёт | 4 / Д−14 | Позже |

Как они устроены ([CONTRIBUTING.md](../../CONTRIBUTING.md#задачи-для-друга)): в `main` заранее лежат падающие тесты
с `[Fact(Skip = "ЗАДАЧА #NN")]` и заглушки `// ЗАДАЧА #NN`. Ты снимаешь `Skip` и добиваешься зелёного, в PR пишешь 5 строк
«своими словами». Не успел к сроку — не страшно: доделает Claude, а ты разберёшь решение. Следующие задачи появятся, когда будут
готовы их тесты.

## 4. Как сделать каждую

Здесь коротко, по шагам — в самих задачах. Где написано «примерно так», интерфейс сервиса мог поменяться: ищи по смыслу.

**1. 2FA на GitHub.** Аватар справа вверху → **Settings → Password and authentication** → включить двухфакторную проверку
через приложение-аутентификатор. Коды восстановления сохрани у себя — не в репозиторий и не в чат.

**2. Supabase** — облачный PostgreSQL. Бесплатно: 500 МБ, пауза после недели простоя (§9).
1. Зарегистрируйся на supabase.com, включи 2FA в настройках аккаунта.
2. Создай **новую** организацию (Free) и в ней **новый** проект `gorodki`. Проекты других приложений не трогаем и не переиспользуем.
3. Регион — в Европе (EU). Какой именно — спроси Никиту **до** создания: потом регион не поменять.
4. Пароль базы — длинный случайный. Сразу сохрани в менеджер паролей: это главный секрет из твоих.
5. Если форма предлагает выбрать про Data API — выбирай вариант без него (только подключение к базе): сервер ходит в базу
   напрямую (§7.3). Выбора нет — не страшно, выключим на спайке S9.
6. PostGIS включать не нужно — его создают миграции сервера ([AppDbContext.cs](../../backend/src/Gorodki.Api/Infrastructure/Persistence/AppDbContext.cs)).
7. Пригласи Никиту в организацию (примерно так: настройки организации → Team → Invite). Email и роль спроси у него лично, не в задаче.
8. Передай пароль — как описано ниже.

Ключи API Supabase (`anon`, `service_role`) нам не нужны — никому их не отправляй. Строку подключения целиком тоже:
адрес Никита видит сам, а какой вариант подключения взять, решит Claude на спайке S9.

**Как передать секрет** (§9: «канал — одноразовая ссылка»):
1. Открой любой сервис одноразовых записок — записка сгорает после первого прочтения.
2. Вставь только сам пароль, без подписи, от чего он.
3. Ссылку пришли Никите в личку, а что это за пароль — отдельным сообщением.
4. Никита открыл — ссылка сгорела. Если он видит «уже прочитано», а сам не открывал — сбрасываешь пароль в Supabase и повторяете.

Никогда не пиши секреты в задачи и PR (репозиторий публичный — их видит весь интернет), в код, в общий чат, на скриншоты.

**3. Сервер и тесты у себя.** Всё по [getting-started-windows.md](getting-started-windows.md): .NET 10 SDK, Git, IDE
(Rider, Visual Studio или VS Code), `git clone`, затем из папки репозитория:

```powershell
dotnet test --solution backend/Gorodki.slnx
dotnet run --project backend/src/Gorodki.Api
```

Открой http://localhost:5080/scalar и вызови `GET /health`. Без Docker интеграционные тесты пропускаются, а без базы сервер
стартует без входа и профиля — так и задумано. Docker Desktop нужен, только если хочешь гонять интеграционные тесты у себя.

**4. Первый PR.**

```powershell
git switch main
git pull
git switch -c chore/codeowners-backend
```

В [.github/CODEOWNERS](../../.github/CODEOWNERS) удали строку-комментарий «Когда друг примет приглашение…» и добавь в конец:

```
/backend/   @Nikich196 @trinotox64
```

Никиту в строке оставляем: в CODEOWNERS побеждает последнее подходящее правило.

```powershell
git add .github/CODEOWNERS
git commit -m "chore(infra): Егор — ревьюер backend/ в CODEOWNERS"
git push -u origin chore/codeowners-backend
```

На странице репозитория нажми **Compare & pull request**, заполни шаблон, в разделе «Зачем» напиши `Closes #<номер задачи>`.
Дождись зелёных проверок. Сливает Никита. После этого GitHub будет сам звать тебя ревьюером на PR, где меняется `backend/`.
Одобрение для слияния не требуется: смотри, когда есть время, и спрашивай в комментариях всё непонятное. Лучшей подготовки к защите не найти.

**5. Sentry.** Зарегистрируйся на sentry.io. **Регион хранения данных — EU**: его выбирают при создании организации,
потом не поменять (§9). Включи 2FA. Создай проект для iOS-приложения: план подключает `sentry-cocoa` без персональных данных (§7.2).
Нужен ли отдельный проект для сервера — спроси Никиту. Ключ проекта (DSN) сохрани; куда его положить, скажет Claude, когда будет
подключать Sentry, а передать его — тем же способом, что и пароль. Сайт не открывается из Беларуси — напиши Никите:
доступность сервисов из РБ план проверяет отдельно (§9).

**6. cron-job.org.** Render Free засыпает без запросов, Supabase Free встаёт на паузу после недели простоя. Поэтому в плане
пингер «с реальным запросом к БД» (§2, D7).
1. Сейчас: заведи аккаунт (и 2FA, если сервис её предлагает).
2. После того как Никита развернёт сервер ([как](deploy-render.md)): новое задание (примерно так: Cronjobs → Create) —
   `GET https://<адрес сервера>/health/ready` **каждые 10 минут** (Render Free засыпает после 15 минут без запросов).
   Этот адрес проверяет базу ([Program.cs](../../backend/src/Gorodki.Api/Program.cs)), а `/health` — нет. Адрес даст Никита,
   уведомления о сбоях отправляй себе на почту.
3. Проверь: в истории задания ответы 200. Запасной вариант по плану — UptimeRobot.
4. Ответ 503 — это сбой, и письмо придёт не зря: в теле ответа видно, что случилось — база недоступна (`database`) или
   заполнена больше чем на 350 МБ из 500 (`storage`, [бюджет хранения](../architecture/data-model.md#бюджет-хранения)).
   Напиши Никите.

**7. Спайк S2b** (§10, этап 1). Критерии: приложение запускается на втором iPhone с другим Apple ID, работают вход через Google
и расширение. Значит, раздать игру одногруппникам реально.
1. Проверь версию iOS: нужна **26.0 или новее**. Ниже — сразу пиши Никите.
2. Дождись, пока Никита пройдёт S2 (#1): если у него не встанет, у тебя тоже.
3. Пройди [install-on-iphone.md](install-on-iphone.md) целиком. Apple ID — твой, но лучше отдельный «расходный», не основной (почему — там же).
4. Пришли Никите три снимка: «Проверка установки», Live Activity на экране блокировки, виджет. Таблицу из шага 4 инструкции
   заполни комментарием к задаче — без Apple ID и личных данных.
5. Вход через Google проверим вторым заходом, когда в приложении появится экран входа, а у Никиты — Google OAuth.

Не вышло с твоим Apple ID — запасной путь из плана: подпись Apple ID Никиты.

**8. Разобраться в сервере.** К защите тебе нужно объяснять конвейер захвата и геометрический контракт (§8). Порядок чтения:
1. [Program.cs](../../backend/src/Gorodki.Api/Program.cs) → `Features/Health/HealthEndpoints.cs` → `Gorodki.Domain/Time/GameClock.cs` и тесты к ним.
2. [runs.md](../architecture/runs.md) и `Features/Runs` — как точки попадают на сервер.
3. [captures.md](../architecture/captures.md) и `Features/Captures/CaptureProcessor.cs` — как заявка петли становится землёй.
4. [ADR 0003](../adr/0003-territory-engine.md), `Gorodki.Domain/Geo/GeoOps.cs`, `Gorodki.Domain/Territory/` — геометрия.
5. Поставь точку останова в тесте из `Gorodki.Domain.Tests/Territory` и пройди его отладчиком (Docker не нужен).
6. Итог — комментарием к задаче: 5–10 строк «своими словами» и список вопросов. Разберём с Никитой и Claude: в плане на обучение
   30 минут в неделю.

## 5. Как работать с репозиторием

- **Один раз:** `git config --global user.name "Trinoto"` и `user.email`. Репозиторий публичный, email из коммитов видят все.
  Можно включить в GitHub **Settings → Emails → Keep my email addresses private** и взять показанный там адрес `…@users.noreply.github.com`.
  Первый `git push` откроет окно входа в GitHub (Git Credential Manager ставится вместе с Git для Windows).
- **Цикл:** задача → ветка латиницей `тип/коротко` (`feat/…`, `fix/…`, `test/…`, `docs/…`, `chore/…`) → коммиты вида
  `feat(backend): описание по-русски` → `git push` → PR по шаблону → зелёный CI → Claude комментирует, Никита сливает (squash) →
  `git switch main` и `git pull`. Одна задача — один PR.
- **`main` защищена:** напрямую не запушить, только через PR с тремя зелёными проверками:
  - `backend` — сборка в Release (любое предупреждение = ошибка), все тесты, включая интеграционные, и Docker-образ для Render;
  - `ios-core` — Swift-пакеты на Linux и оформление Swift-кода;
  - `ios-build` — сборка приложения на Mac. Если `ios/` не менялась, проверка пропускается и считается пройденной.
- **Проверить у себя, как в CI** (предупреждения станут ошибками):
  `$env:CI = "true"; dotnet build backend/Gorodki.slnx -c Release; Remove-Item Env:CI`
- **Правила кода:** имена по-английски, комментарии по-русски. Время — только через `TimeProvider`/`GameClock`. Числа правил — в
  игровом конфиге, не в коде. Версии NuGet — только в `backend/Directory.Packages.props`. `contracts/*.json` руками не правь:
  как их менять, описано в [contracts/README.md](../../contracts/README.md).
- **Нельзя:** секреты в коде, задачах и PR (если секрет попал в коммит — сразу скажи Никите: ключ перевыпускаем, удаления из
  истории мало); треки и данные игроков; координаты тайников; сливать свои PR самому. Не трогай другие репозитории и
  проекты Никиты и чужие проекты Supabase.

## 6. Где что лежит

| Путь | Что там |
|---|---|
| [docs/PLAN.md](../PLAN.md) | Главный план. Тебе нужны §1 (команда), §8 (задачи для друга), §9 (аккаунты), §10 (этапы) |
| [docs/JOURNAL.md](../JOURNAL.md) | Что сделано; сверху — текущий этап, следующий шаг, блокеры |
| [docs/architecture/](../architecture/) | Как устроен сервер: вход, забеги, захваты, карта, туман, модель данных, синхронизация |
| [docs/adr/](../adr/README.md) | Почему решили так, а не иначе |
| [docs/guides/](.) | Инструкции: Windows, iPhone, пробная прогулка, этот файл |
| `backend/src/Gorodki.Domain` | Чистые правила игры без базы и ASP.NET: `Geo`, `Territory`, `Runs`, `Fog`, `Time`, `Leagues`, `Config` |
| `backend/src/Gorodki.Api` | Веб-сервер: `Program.cs`, `Features/<Название>/` (по папке на возможность), `Infrastructure/Persistence` (EF Core, миграции) |
| `backend/tests/` | `Gorodki.Domain.Tests`, `Gorodki.Api.Tests` (сервер в памяти), `Gorodki.IntegrationTests` (Docker + образ Supabase) |
| `backend/tools/Gorodki.GeoBench` | Замер скорости движка участков |
| `backend/Dockerfile`, `global.json` | Образ для Render; версия .NET SDK (10.0.401) |
| [contracts/](../../contracts/README.md) | Общие файлы сервера и телефона: конфиг, OpenAPI, эталоны тумана и судьи |
| `ios/` | Приложение: `project.yml`, `App/`, `Widgets/`, `Packages/` (GameCore, GorodkiAPI, GorodkiKit, GorodkiSync) |
| [.github/](../../.github/) | CI (`workflows/`), шаблоны задач (есть «Задача для друга») и PR, `CODEOWNERS` |
| [CONTRIBUTING.md](../../CONTRIBUTING.md) | Правила: ветки, коммиты, метки, безопасность |

## 7. Чем Claude уже помог

- Составил план (исследование и критика несколькими агентами) и ведёт журнал.
- Создал и настроил репозиторий: структура, CONTRIBUTING, шаблоны задач и PR, CODEOWNERS, Dependabot, метки, вехи «Этап 0…6»,
  защита `main`, первые задачи.
- Настроил CI: `backend`, `ios-core`, `ios` и `ipa` (собирает IPA для Sideloadly).
- Написал весь серверный код из раздела 2 и тесты к нему. Перед крупными шагами отдаёт дизайн на критику отдельному агенту,
  а тесты проверяет мутациями: намеренно ломает код и смотрит, ловят ли тесты поломку.
- iOS: каркас приложения, «Лаборатория», GameCore, клиент API, офлайн-синхронизация.
- Документация: архитектура, ADR, инструкции (Windows, iPhone, прогулка) и этот файл.

## 8. Если что-то не получается

- Пиши Никите в общий чат или в личку. Вопрос по коду можно оставить комментарием в задаче или PR — Никита передаст его Claude.
- Приложи: номер задачи и шаг; что делал (команду); текст ошибки целиком или скриншот; `dotnet --version`; ссылку на упавший запуск CI.
- Перед отправкой убери из текста и скриншотов пароли, ключи, токены, email и Apple ID: задачи публичные.
- Нужно решение Никиты — задача с меткой `нужно-решение-Никиты`. Лучше спросить, чем угадать.

**Пока не решено (спросить Никиту):** какой регион EU для Supabase и какая роль у Никиты там; нужен ли проект Sentry для сервера;
адрес и интервал пингера (после деплоя); пишешь ли ты главы записки (ждём ответа преподавателя, #3).
